{
  "name": "knowledge-base",
  "version": "0.3.0-dev",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "bun test",
    "typecheck": "bun --bun tsc --noEmit",
    "lint": "biome check packages",
    "format": "biome format --write packages"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.3.0",
    "typescript": "^5.6.3"
  }
}
